#include <bits/stdc++.h> 
using namespace std;

void findCount(int arr[],int n,int sum)
{
    int i,temp;
    set<int>s;
    for(i=0;i<n;i++)
    {
        temp=sum-arr[i];
        if(s.find(temp)!=s.end())
        cout<<"["<<arr[i]<<" "<<temp<<"]"<<endl;
        s.insert(arr[i]);
    }
}


int main()
{
	int n,i,sum;
	cin>>n>>sum;
	int arr[n];
	for(i=0;i<n;i++)
	cin>>arr[i];
	findCount(arr,n,sum);
	return 0;
}

***********************************
I/P:
6 16
9 10 4 7 6 12
O/P:
[7 9]
[6 10]
[12 4]
