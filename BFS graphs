#include <stdio.h>
#define max 5
void bfs(int adj[][max],int visited[],int start);
//BFS algorithmm for graphs
int main()
{
    int visited[max]={0};
    int adj[max][max],i,j;
    for(i=0;i<max;i++)
    for(j=0;j<max;j++)
    scanf("%d",&adj[i][j]);
    bfs(adj,visited,0);
    
	return 0;
}

void bfs(int adj[][max],int visited[],int start)
{
    int queue[max],rear=-1,front=-1,i;
    queue[++rear]=start;
    visited[start]=1;
    while(rear!=front) //while queue isnt empty
    {
        start=queue[++front]; //dequeue the queue;
        printf("%c ",start+65); //print the elements
        for(i=0;i<max;i++)
        {
            if(adj[start][i]==1 && visited[i]==0)
            {
                queue[++rear]=i;
                visited[i]=1;
            }
        }
    }
}
