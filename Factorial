#include<stdio.h>

int fact(int n)
{
    if(n==1)
    return 1;
    else if(n<0)
    return 0;
    else
    return n*fact(n-1);
}
int main()
{
    int n,res;
    scanf("%d",&n);
    res=fact(n);
    printf("%d",res);
    return 0;
}
//input-5
output-120
